const express = require("express");

const authRouter = express.Router();

// router.post("/signup");

// router.post("/login");

// router.post("/logout");

// router.get("/current",);

module.exports = authRouter;